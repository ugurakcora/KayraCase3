import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Multi-Zone Architecture için basePath ve rewrites kullanılabilir
  // Şu an için her uygulama bağımsız çalışacak
};

export default nextConfig;
